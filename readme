 Installation

1. Créer un environnement virtuel (Python 3.9 requis) :
   python -m venv venv
   venv\Scripts\activate

   pip install rasa==3.5.11

2. Démarrage du bot
rasa train        # Entraîne le modèle
rasa shell        # Lance le bot en mode console
